---
import { profile } from "../data/profile";
---

<section class="hero" id="hero" data-section="hero" data-reveal tabindex="-1">
  <div class="hero-surface">
    <div class="hero-media" data-parallax data-animate>
      <span class="avatar avatar--hero" data-avatar data-initials="AM">
        <img
          src={profile.headshot.url}
          alt={profile.headshot.alt}
          width={profile.headshot.width}
          height={profile.headshot.height}
          loading="eager"
          decoding="async"
          fetchpriority="high"
          data-fallback={profile.fallback.src}
        />
        <span class="avatar-fallback" aria-hidden="true">AM</span>
      </span>
      <div class="hero-orb" aria-hidden="true"></div>
    </div>
    <div class="hero-copy content" data-animate-group>
      <p class="hero-kicker" data-animate>Cloud &amp; Platform Engineer</p>
      <h1 data-animate>{profile.name}</h1>
      <p class="hero-tagline" data-animate>
        Building reliable platforms that empower teams to ship with confidence.
      </p>
      <div class="flex w-full flex-wrap gap-4 md:w-auto" data-animate>
        <a class="btn" href="#projects" data-scroll>View Featured Work</a>
        <a class="btn ghost" href="#contact" data-scroll>Let's Collaborate</a>
      </div>
    </div>
  </div>
</section>
